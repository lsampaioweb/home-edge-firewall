---
- name: "Firewall Policy settings"
  hosts: "firewalls"
  gather_facts: false

  tasks:
    - name: "Creating Firewall Policy entries from backup"
      ansible.builtin.include_role:
        name: "firewall_policy"
        tasks_from: "create-from-backup.yml"

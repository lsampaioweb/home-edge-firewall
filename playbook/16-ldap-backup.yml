---
- name: "LDAP settings"
  hosts: "firewalls"
  gather_facts: false

  tasks:
    - name: "Backing up the LDAP settings"
      ansible.builtin.include_role:
        name: "ldap"
        tasks_from: "backup.yml"

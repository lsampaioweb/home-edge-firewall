---
- name: "DHCP settings"
  hosts: "firewalls"
  gather_facts: false

  tasks:
    - name: "Backing up the DHCP settings"
      ansible.builtin.include_role:
        name: "dhcp"
        tasks_from: "backup.yml"

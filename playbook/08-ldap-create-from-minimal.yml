---
- name: "LDAP settings"
  hosts: "firewalls"
  gather_facts: false

  tasks:
    - name: "Creating LDAP entries from minimal JSON"
      ansible.builtin.include_role:
        name: "ldap"
        tasks_from: "create-from-minimal.yml"

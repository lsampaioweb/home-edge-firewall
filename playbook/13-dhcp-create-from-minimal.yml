---
- name: "DHCP settings"
  hosts: "firewalls"
  gather_facts: false

  tasks:
    - name: "Creating DHCP entries from minimal JSON"
      ansible.builtin.include_role:
        name: "dhcp"
        tasks_from: "create-from-minimal.yml"

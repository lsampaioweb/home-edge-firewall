---
- name: "SNMP settings"
  hosts: "firewalls"

  tasks:
    - name: "Creating SNMP entries from backup"
      ansible.builtin.include_role:
        name: "snmp"
        tasks_from: "create-from-backup.yml"

---
# lookup('env', 'HOME') runs in the Ansible process context (the invoking user),
# so it is unaffected by become: true and always returns the real user's home dir.
pipx_executable: "{{ lookup('env', 'HOME') }}/.local/bin/pipx"

pipx_packages:
  # Personal Development Environment - Ansible tools for infrastructure management.
  - {name: "ansible", install_deps: true}
  - {name: "ansible-lint", install_deps: true}
  - {name: "ansible", state: "inject", include_injected: true, inject_packages: ["cryptography", "jmespath", "hvac", "certifi", "passlib"]}

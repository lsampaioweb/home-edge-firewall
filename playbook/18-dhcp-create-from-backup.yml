---
- name: "DHCP settings"
  hosts: "firewalls"
  gather_facts: false

  tasks:
    - name: "Creating DHCP entries from backup"
      ansible.builtin.include_role:
        name: "dhcp"
        tasks_from: "create-from-backup.yml"

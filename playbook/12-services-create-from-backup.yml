---
- name: "Services settings"
  hosts: "firewalls"

  tasks:
    - name: "Creating Services from backup"
      ansible.builtin.include_role:
        name: "services"
        tasks_from: "create-from-backup.yml"

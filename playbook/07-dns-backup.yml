---
- name: "DNS settings"
  hosts: "firewalls"
  gather_facts: false

  tasks:
    - name: "Backing up the DNS settings"
      ansible.builtin.include_role:
        name: "dns"
        tasks_from: "backup.yml"

---
- name: "SNMP settings"
  hosts: "firewalls"
  gather_facts: false

  tasks:
    - name: "Backing up the SNMP settings"
      ansible.builtin.include_role:
        name: "snmp"
        tasks_from: "backup.yml"
